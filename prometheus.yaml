global:
  scrape_interval: 120s
  evaluation_interval: 120s

scrape_configs:
  - job_name: "prometheus"
    scrape_interval: 5s
    static_configs:
      - targets:
          - "prometheus:9090"

  - job_name: "Gym Application input"
    metrics_path: "/actuator/prometheus"
    scrape_interval: 2s
    static_configs:
      - targets:
          - "gym-app:8081"
        labels:
          application: "Gym Application"